import React from "react";
import styled from "styled-components";

import { Header } from "./styled";

const Root = styled.div``;

export default () => (
  <React.Fragment>
    <Header label="next steps" />
    <Root />
  </React.Fragment>
);
